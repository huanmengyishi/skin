# Release Notes — dsh-skin v1.5.0

- 版本：v1.5.0（package.json / Git tag / GitHub Release 三者一致）
- 验收证据：run-015（AI Skin Lifecycle）；历史证据 run-009~run-014（本地档案 D:\deepskin\图库\dsh-skin-test\，不入本仓库）
- 验证摘要：单元/契约/集成 232/232（40 文件）；E2E 9/9；A/B/C/D/G/H 实跑；Q1~Q5、v1.3 确定性、v1.4 修复环回归 PASS；生产环境 untouched

## Highlights

- AI Skin Lifecycle：创建 → 生成（v1.4 闭环）→ 安装 → 重新生成（版本+1/父子链）→ 取消 → 崩溃恢复 → 元数据/设计编辑 → 导出 → 卸载/重装
- 确定性构建（v1.3）与修复闭环（v1.4）作为统一 Generation Service 消费，不复制实现
- 失败安全不变量：任何失败/取消/崩溃，上一有效版本保持

## Provenance

skinId → package（packageHash）→ generation（generationId/parent）→ spec（specHash）→ evidence（evidenceHash）→ 参考图（inputHash）
